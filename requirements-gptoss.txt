# gpt-oss-20b inference dependencies
# Requires the custom gptoss vLLM fork — NOT compatible with the cosmos venv.
# Installed via pip (not uv) to avoid cache-mount I/O failures on large CUDA wheels.
# torch is pulled automatically as a vllm dependency from the nightly cu128 index.
#
# vllm wheel source: https://wheels.vllm.ai/gpt-oss/
transformers>=4.57.0
kernels
vllm==0.10.1+gptoss
huggingface_hub[cli]>=0.23.0
